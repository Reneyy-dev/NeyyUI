local NeyyUI = {}

function NeyyUI:CreateWindow(config)
    config = config or {}

    print("[NeyyUI] CreateWindow berhasil")
    print("[NeyyUI] Title:", config.Title or "Untitled")

    local Window = {}

    return Window
end

return NeyyUI
